# Remind to sign commits/tags at commit time (not when pushing a PR).
git config --local commit.gpgSign true
git config --local tag.gpgSign true

dotenv_if_exists
# COMPACTC_VERSION etc. for the compact-js toolchain (also loaded in CI).
dotenv packages/compact-js/compact.env
